// WhatIfDeathScene intentionally removed — stub placeholder.
"use client";

import React from 'react';

const WhatIfDeathScene: React.FC<any> = () => {
  if (typeof window !== 'undefined') {
    // eslint-disable-next-line no-console
    console.warn('WhatIfDeathScene is stubbed/removed.');
  }
  return null;
};

export default WhatIfDeathScene;
